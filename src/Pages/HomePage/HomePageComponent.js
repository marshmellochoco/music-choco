import { PlaylistContainer } from "../../common/Playlist/PlaylistContainer";

export const HomePageComponent = () => {
    return (
        <div>
            <PlaylistContainer />
        </div>
    );
};
